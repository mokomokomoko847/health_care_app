# PROJECT_RULES.md

# プロジェクト名

楽ちん報告アプリ（仮）

---

# 1. アプリの目的

診察時に、お医者さんへ生活状況を説明する手間を減らすための
シンプルな健康ログアプリをFlutterで開発する。

今回開発するのは「プロトタイプ1（MVP）」である。

将来的には蓄積したデータをAI研究などへ活用する構想があるが、
AIによる予測・診断・合併症の予兆検知などは今回実装しない。

---

# 2. 開発技術

- Framework: Flutter
- Language: Dart
- 対象: スマートフォンアプリ
- 開発段階: Prototype 1 / MVP

データベースや画像保存方式などの技術選定はCodexから提案してよい。

ただし以下を優先すること。

1. 可能な限り無料（0円）
2. プロトタイプとして実装が複雑になりすぎない
3. Flutterとの相性が良い
4. アプリ再起動後もデータを保持できる
5. 将来的にデータ構造を拡張しやすい

有料サービスが必要になる場合は、勝手に導入せずHumanへ確認すること。

---

# 3. 1件の記録として保存する項目

1件の記録は以下の項目を持つ。

## 日時

- 必須
- 現在日時を初期値として自動取得する
- ユーザーによる日時変更を可能にする

## 血糖値

- 必須
- 整数のみ
- 最小値: 0
- 最大値: 600
- 単位: mg/dL 固定
- 小数は許可しない
- 未入力は許可しない

## 測定タイミング

以下の2択。

- 食前
- 食後

必須項目とする。

## メモ

- 任意入力
- 未入力を許可する
- 最大50文字（Prototype 1の仮仕様）

血糖値が高かった・低かった場合などに、
ユーザーが補足情報を入力する目的で使用する。

## 食事写真

- 1件につき1〜3枚
- 最大3枚
- カメラで新しく撮影できる
- 端末内の既存写真から選択できる

---

# 4. 履歴表示

履歴画面は以下の流れとする。

カレンダー
↓
日付を選択
↓
その日の記録一覧

例:

8月15日を選択

12:30
血糖値: 120 mg/dL
食前
[食事写真]
メモ: ○○○

19:00
血糖値: 145 mg/dL
食後
[食事写真]
メモ: ○○○

同じ日に複数の記録が存在することを前提とする。

---

# 5. Prototype 1で実装しないもの

以下は今回のMVPから明確に除外する。

- 新規ユーザー登録
- ログイン
- ログアウト
- AIによる予測
- AIによる診断
- 合併症の予兆検知
- その他、Prototype 1の完成に不要な高度な機能

これらをCodexの判断で追加実装しないこと。

---

# 6. Prototype 1の完成条件

以下がすべて動作すればPrototype 1完成とする。

- 血糖値を入力できる
- 血糖値の入力ルール（整数・0〜600）が機能する
- 現在日時が初期値として自動取得される
- 日時をユーザーが変更できる
- 「食前」「食後」を選択できる
- メモを入力できる
- 食事写真をカメラで撮影できる
- 端末内の既存写真を選択できる
- 写真を最大3枚扱える
- 記録を保存できる
- カレンダーから日付を選択できる
- 選択した日の記録を確認できる
- 同じ日の複数記録を確認できる
- アプリを再起動しても保存データが残っている

---

# 7. Codexの作業ルール

## 最重要ルール

必ず1タスクずつ実行すること。

複数の開発タスクをHumanの許可なく連続して実行しない。

基本フロー:

1. 次に実行するタスクを確認する
2. 実装内容を簡潔に説明する
3. Humanの許可を得る
4. そのタスクだけ実装する
5. 必要なテストを行う
6. Humanへ完了報告する
7. HumanによるFlutterでの動作確認を待つ
8. HumanからOKが出るまで次のタスクへ進まない

Humanから修正指示があった場合は、
次のタスクへ進まず現在のタスクを修正すること。

---

# 8. Humanへ確認する事項

以下の場合はCodexだけで決定せずHumanへ確認すること。

- アプリの仕様を変更する場合
- 新しい機能を追加する場合
- Prototype 1のスコープを変更する場合
- 有料サービスを使用する場合
- 既存データを削除する可能性がある場合
- 大きな設計変更を行う場合
- 要件が矛盾している場合
- PROJECT_RULES.mdだけでは判断できない重要事項がある場合

---

# 9. Codexに任せてよい事項

以下の技術的判断についてはCodexから提案してよい。

- Flutterのファイル構成
- クラス構成
- Widgetの選択
- 必要なFlutter package
- データベース候補
- 写真保存方式
- 状態管理方法
- コードの整理
- テスト方法
- カレンダー実装方法

ただし大きな技術選定については、
採用理由をHumanへ説明してから進めること。

---

# 10. Flutter学習のためのルール

HumanはFlutter/Dart初心者であり、
このプロジェクトを通してFlutterを理解することも目的としている。

そのためコードを必要以上に複雑にしないこと。

可能な限り、

- 読みやすいコード
- 初心者にも追いやすい構造
- 適切な変数名
- 適切なクラス名
- 適切なコメント

を使用すること。

生成・変更した重要なFlutter/Dartコードには、
「何のための処理なのか」が初心者にも分かる日本語コメントを付けること。

ただし、単純な括弧や明白な構文まで機械的に1行ずつコメントして
コードを読みにくくしないこと。

Humanから質問された場合は、

「何をしているコードか」
「なぜ必要なのか」
「Flutter/Dartのどの仕組みを使っているのか」

を説明できる状態にすること。

---

# 11. タスク完了時の報告

各タスク完了時に以下をHumanへ報告すること。

## 実装したこと

今回何を実装したか。

## 変更したファイル

作成・変更したファイル一覧。

## コードのポイント

初心者が今回理解するとよいFlutter/Dartのポイント。

## テスト結果

Codex側で実行した確認・テストとその結果。

## Humanが確認すること

Flutterを起動したHumanが、
実際にどこを操作して何を確認すればよいか。

---

# 12. 次タスクへの移行

タスク完了後に勝手に次のタスクを実装しない。

Humanから

「OK」
「完了」
「次へ進んでください」

などの明確な承認を得てから次のタスクへ進むこと。